pipeline {
    agent any
   env 
	{
		path=$(which docker) docker-compose up
	}
    stages {
        stage('Integration Test') {
		 steps {
		
                sh "path"
		
                
           	 }


            }
	
        
    }
}