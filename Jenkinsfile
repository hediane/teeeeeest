pipeline {
    agent any
    
    stages {
        stage('Integration Test') {
		 steps {
		sh "cd teeeeeest"
                sh "docker-compose up --force-recreate --abort-on-container-exit"
                
           	 }


            }
	
        
    }
}